#include <iostream>
using namespace std;

int main() {
    int num1, num2, sum;
    cout << "Enter two integers: ";
    cin >> num1 >> num2; // Takes two inputs from the user

    sum = num1 + num2;
    cout << "The sum is: " << sum << endl;
    return 0;
}
